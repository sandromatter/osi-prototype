{% set navsMain = [
    { title: 'Home', url: 'index.html', state: 'home' },
    { title: 'Über', url: 'ueber.html', state: 'ueber' },
    { title: 'Kontakt', url: 'kontakt.html', state: 'kontakt' }
] %}

<nav class="navbar nav-main navbar-expand-lg navbar-light mb-0">
  <a class="navbar-brand" href="index.html">
    <img src="/img/OSI_Logo.png" alt="OSI Logo">
  </a>
  <a class="avatar" href="user.html">
    <img class="rounded-circle" src="/img/Profilbild.jpg" alt="User avatar">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">

        {% for nav in navsMain %}
            {% if page == nav.state %}
            <li class="active">
            {% else %}
            <li>
            {% endif %}
                <a class="nav-item nav-link" href="{{ nav.url }}">{{ nav.title }}</a>
            </li>
        {% endfor %}
        
    </div>
  </div>
</nav>